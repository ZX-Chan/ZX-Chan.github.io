---
title: "CH21 地址映射、差错报告和多播"
description: "Computer Networks 笔记"
slug: "ch21-地址映射-差错报告和多播"
date: "2025-12-25"
categories:
    - "Computer Networks"
tags:
    - "notes"
---

# CH21 地址映射、差错报告和多播
## 地址映射
物理地址和IP地址的映射
- 静态映射
	- 存一个表，但是如果一个电脑换了网卡可能物理地址就不对了
- 动态映射
	- 用协议求
### ARP
- 必须知道目标主机的MAC地址才能封装成帧。
- 发送一个ARP请求，用IP地址求MAC地址，只有IP地址对的上的主机才会回应。
#### 高速缓存器
A可能会发送多个分组到B，不能每次都发个ARP请求吧太低效了，于是会把收到的ARP回复先存在高速缓存里一段时间，在之后发送ARP请求前先看看高速缓存里有没有。
#### 分组格式
![](/images/notes/计算机网络/Pasted image 20241231202051.png)
#### 代理ARP
- 代表一组主机的ARP
- 如果收到针对这些主机的ARP请求，就把自己的硬件地址发回去
- 收到IP分组后发送给对应的主机
### RARP BOOTP DHCP
#### 反向地址解析协议RARP
好像不用看
#### 引导程序协议BOOTP
- 应用层协议
- 物理地址到逻辑地址的映射
- 。。。再说
#### 动态主机配置协议DHCP
- 提供可以是人工或自动的静态或动态的地址配置
## ICMP
### 报文类型
- 差错报告报文
- 查询报文
### 报文格式
- 8字节的头部和一个可变长的数据部分
### 差错报告
- IP是不可靠的，ICMP的主要职责之一就是差错报告
- 不纠错，只是报告差错
- 向原始的源方报告差错报文
- 总共五种
	- 终点不可达：当路由器或主机不能交付数据时就向源点发送终点不可达报文。
	- 源站抑制：当路由器或主机由于拥塞而丢弃数据报时，就向源点发送源点抑制报文，使源点知道应当把数据报的发送速率放慢
	- 超时：当路由器收到生存时间为零的数据报时，除丢弃该数据外，还要向源点发送时间超时报文。当终点在预先规定的时间内不能收到一个数据报的全部数据报分片时，就把已收到的数据报分片丢弃，并向源点发送时间超时报文
	- 参数问题：当路由器或目的主机收到的数据报的首部中有的字段的值不正确时，就丢弃该数据报，并向源点发送参数问题报文
	- 重定向：路由器把改变路由报文发送给主机，让主机知道下次应将数据报发送给另外的路由器（可通过更好的路由）
- 差错报文特点
	- 对于携带ICMP差错报文的数据报，不再产生ICMP差错报文
	- 对于分段的数据报文，如果不是第一个分段则不产生ICMP差错报文
	- 对于多播地址的数据报文，不产生ICMP 差错报文
	- 具有特殊地址的数据报文，如127.0.0.0或者0.0.0.0，不产生ICMP差错报文
- 差错报告的格式
![](/images/notes/计算机网络/Pasted image 20250101160406.png)
### 查询报文
- 四种
	- 回送请求和回答报文
	- 时间戳请求和回答报文
	- 掩码地址请求和回答报文
	- 路由器询问和通告报文
- 好像不是很重要

### 校验和
加起来取反

## IGMP
不考

